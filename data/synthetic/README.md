# Synthetic Data

`cooccurrence_sessions.csv` is generated by:

```bash
python -m src.textthreat.cooccurrence --sample
```

It supports the thesis synthetic session-window co-occurrence evaluation. It is not a real social-media session dataset.
